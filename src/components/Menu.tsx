import React, { useState } from 'react'

const Menu = () => {
  const [menuOpen, setMenuOpen] = useState(false)

  return (
    <div>
      <button>
      
      </button>
    </div>
  )
}

export default Menu